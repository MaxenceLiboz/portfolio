import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles({
    button: {
        backgroundColor: "#252122",
        "&:hover": { opacity: "0.95", backgroundColor: "#252122" },
    },
});

export default useStyles;
